FROM postgres:18-alpine

RUN apk add --no-cache \
        age \
        aws-cli \
        bash \
        ca-certificates \
        tzdata

ENTRYPOINT []
CMD ["sh", "-c", "echo 'backup service placeholder — implementation in subsequent commits'; exit 0"]

WORKDIR /backup
