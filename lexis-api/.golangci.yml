version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - bodyclose
    - noctx

  settings:
    errcheck:
      check-type-assertions: true

issues:
  exclude-dirs:
    - internal/db
